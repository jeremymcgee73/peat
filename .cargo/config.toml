# Use mold linker on Linux for faster and more stable linking
# lld can crash with "Bus error" on large projects in CI
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
