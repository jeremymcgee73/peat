rootProject.name = 'peat-atak-plugin'
include ':app'

// Use local peat-btle repo for development
includeBuild('../../peat-btle/android') {
    dependencySubstitution {
        substitute(module('com.defenseunicorns:peat')).using(project(':'))
    }
}
