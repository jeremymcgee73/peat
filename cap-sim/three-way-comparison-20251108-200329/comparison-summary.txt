THREE-WAY MODE COMPARISON RESULTS
Timestamp: 20251108-200329
Topology: 24-node platoon (client-server)

========================================
TEST CONFIGURATION
========================================

Mode 1: Traditional IoT Baseline
  - Binary: traditional_baseline
  - Sync: None (periodic full broadcasts)
  - Query: N/A
  - Expected: Highest bandwidth, worst latency

Mode 2: CAP Full Replication
  - Binary: cap_sim_node
  - Sync: Ditto CRDT
  - Query: Query::All (CAP_FILTER_ENABLED=false)
  - Expected: CRDT overhead + n-squared replication

Mode 3: CAP Differential
  - Binary: cap_sim_node
  - Sync: Ditto CRDT
  - Query: Query::Custom with role filtering (CAP_FILTER_ENABLED=true)
  - Expected: CRDT overhead + filtered replication

========================================
ANALYSIS INSTRUCTIONS
========================================

1. Convergence Time:
   - Search logs for "converged" or final sync timestamps
   - Compare: Mode 1 vs Mode 2 vs Mode 3

2. Query Verification:
   Mode 2 logs should show: "Using full replication (Query::All)"
   Mode 3 logs should show: "Using CAP-filtered query for role: [soldier|uav|ugv]"

3. Bandwidth Analysis:
   - Count sync messages per node
   - Measure bytes transmitted (if logged)
   - Expected: Mode 1 > Mode 2 > Mode 3

4. Document Count:
   - Mode 2: Each node should receive ~24 documents
   - Mode 3: Each node should receive fewer (filtered by role)
   - Check logs for "received document" or sync counts

========================================
NEXT STEPS
========================================

1. Analyze logs in each mode directory
2. Extract metrics (convergence, latency, bandwidth)
3. Create graphs comparing all three modes
4. Document findings in ADR-015
5. Decision: Does Mode 3 show improvement over Mode 2?

========================================
HYPOTHESIS TO VALIDATE
========================================

H1: Mode 3 bandwidth < Mode 2 bandwidth (30-50% reduction)
H2: Mode 3 latency < Mode 2 latency (filtering reduces sync overhead)
H3: Mode 3 convergence ≈ Mode 2 convergence (similar for n-squared)
H4: All modes > Mode 1 (CRDT is better than periodic broadcast)

